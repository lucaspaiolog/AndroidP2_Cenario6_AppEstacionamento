<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppEstacionamento"
        tools:targetApi="31">

        <activity
            android:name=".view.activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppEstacionamento.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Outras Activities -->
        <activity
            android:name=".view.activities.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.AppEstacionamento.NoActionBar" />
        <activity
            android:name=".view.activities.RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.AppEstacionamento.NoActionBar" />
        <activity
            android:name=".view.activities.DriverDashboardActivity"
            android:exported="false"
            android:label="Painel do Motorista" />
        <activity
            android:name=".view.activities.AdminDashboardActivity"
            android:exported="false"
            android:label="Painel do Administrador" />

        <receiver
            android:name=".view.receivers.ReservationAlarmReceiver"
            android:enabled="true"
            android:exported="false" />

    </application>

</manifest>